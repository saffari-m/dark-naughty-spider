import { global } from "../common/global";
import { color } from "../common/color";

export const skeleton = {
    borderRadius: global.borderRadius,
    backgroundColor: color.tertiary,
    shimmerColor: color.secondary.dark,
  };