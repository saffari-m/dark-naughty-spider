import { color } from "../common/color";
import { gradient } from "../common/gradient";

export const card = {
  defaultBackgroundColor: color.neutral900,
  borderColor: color.transLightXS,
  borderRadius: "0.125rem",
  gradient: gradient.neuralFromBottom,
};
